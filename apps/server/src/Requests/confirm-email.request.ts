const authorize = () => {};

const validate = () => {};

export interface ConfirmEmailRequest extends Request {}

const confirmEmailRequest = {
  validate,
  authorize,
};

export default confirmEmailRequest;
