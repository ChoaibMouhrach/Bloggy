{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "start": {
      "dependsOn": ["^build"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "./dist"]
    },
    "lint:fix": {},
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
